#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	char str1[100];
	while(gets(str))
	{
		int l=strlen(str);
		int i,ans=0;
		for(i=l-1;i>=0;i--)
		{
			str1[ans++]=str[i];
		}
		puts(str1);
	}
	return 0;
}
