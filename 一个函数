#include<stdio.h>
int a[100000];
int main()
{
	int n;
	while(scanf("%d",&n)!=EOF)
	{
		int i,count=0;
		for(i=0;i<n;i++)
		{
			scanf("%d",&a[i]);
			count+=a[i];
		}
		printf("%d",count);
	}
	return 0;
}
